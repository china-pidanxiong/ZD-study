<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	 http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd    
            http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd    
            http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

	<!-- 注解扫描路径 -->
	<context:component-scan base-package="cn.ssm.web.controller" />

	<!-- 表示要注册两个对象 DefaultAnnotationHandlerMapping与AnnotationMethodHandlerAdapter 
		这两个对象可以帮助我们做好多的工作如：类型转换，静态资源放行，以及国际化等。。。 -->
	<mvc:annotation-driven />

	<!-- 拦截器
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/user/**" />
			<bean class="com.cars.pojo.MyInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>
 	-->

	<!-- 放行的静态资源路径 -->
	<mvc:resources location="/WEB-INF/images/" mapping="/images/**" />
	<mvc:resources location="/WEB-INF/css/" mapping="/css/**" />
	<mvc:resources location="/WEB-INF/js/" mapping="/js/**" />
	<mvc:resources location="/WEB-INF/upload/" mapping="/upload/**" />


	<!-- 视图解析器 -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- <property name="prefix" value="/WEB-INF/jsp/" />
		<property name="suffix" value=".jsp" /> -->
		<property name="prefix" value="/WEB-INF/html/"/>
		<property name="suffix" value=".html" />
	</bean>

	<!-- 文件上传 -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="utf-8" /> <!-- 默认编码 (ISO-8859-1) -->
		<property name="maxInMemorySize" value="1024" /> <!-- 最大内存大小 (10240) -->
		<property name="uploadTempDir" value="/temp/" /> <!-- 上传后的目录名 (WebUtils#TEMP_DIR_CONTEXT_ATTRIBUTE) -->
		<!-- 这里限制的最大上传文件为5M (5*1024*1024) -->
		<property name="maxUploadSize" value="5242880" /> <!-- 最大文件大小，-1为无限止(-1) -->
	</bean>

</beans>